#7. Extract the first and last elements of your vector from (6.), storing them as a new object.
first_last_elements <- c(final_vector2[1], final_vector2[length(final_vector2)])  
print(first_last_elements)
